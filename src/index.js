import './style.css';
import { createDropdown } from './dropdown';
import { createImageSlider } from './createImageSlider';

const buttonArray = ['Red', 'Blue', 'Green', 'Yellow', 'Purple', 'White'];
createDropdown(buttonArray);

createImageSlider();
